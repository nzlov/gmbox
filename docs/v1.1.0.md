# 1.1.0

## 主题切换

- 在顶部最右边添加切换主题图标
- 点击切换弹窗选择主题
- 添加常用主题
- 允许自定义色彩
- 选择的主题保存到数据库,以便跨设备应用主题。

## 聚合消息

- 直接显示所有的邮件列表,不区分来源邮箱、文件夹
- 按照时间倒序
- 邮件列表参考就聚合消息显示，点击后展开，显示操作和内容。
- 没有分页，滚动到底部加载更多。
- FAB按钮是点击刷新

## 联系人

- 布局保持与旧版聚合消息一致。
- 发件人非已系统添加的邮箱。
- 修改左侧显示为所有的发件人列表，不区分来源邮箱、文件夹。从上往下是：搜索、全部信件、联系人列表。联系人列表按最新邮件的时间倒序。
- 发件人列表顶部还是搜索，筛选发件人。
- 发件人列表主显示发件人姓名，小标题显示发件邮箱。
- 右侧显示所有的邮件。
- 当点击左侧发件人时，邮件列表显示所有与这个发件人所有的往来邮件。按照时间倒序。
- 直接展开邮件内容，不需要点击，去掉发件人姓名。
- 依然分页显示。
- 原搜索位置添加写信按钮，弹窗默认给选择的发件人写信。
- 每个邮件后面有一个回复按钮，弹窗回复信件。
- 联系人列表置顶全部信件条目，用于按时间倒序显示所有联系人的信件。
- FAB改为点击写信。当鼠标悬浮或者触屏长按时使用悬浮显示刷新按钮。

## 邮件列表(旧聚合消息改名邮件列表)

- 去掉搜索按钮
- 文件夹常用的改为中文，并按照常用的顺序排序。
- 将分页大小、邮件统计及上一页、下一页按钮放到分页中间。顺序为：上一页、分页大小、第几页、共多少封、下一页。
- FAB改为点击写信。当鼠标悬浮或者触屏长按时使用悬浮显示刷新按钮。
- 邮件列表点击后展开，显示操作和内容,不再跳页,操作按钮使用图标。
- 标记已读、未读、删除、显示图片、移动都使用图标。
- 移动点击后弹窗选择移动位置。

## 邮件详情页

- 单独的邮件详情页去掉

## 写信

- 去掉写信页面、侧边栏条目。

## 邮箱管理

- FAB改为点击添加邮件，当鼠标悬浮或者触屏长按时使用悬浮显示导入按钮
- 列表批量操作按钮如果没有批量选择的清空下隐藏
- 搜索不要前端过滤，而是放到后端处理。使用防抖处理请求。
- 搜索和批量操作的按钮在同一行

## 同步日志

添加同步日志查看功能，列表需要分页。默认时间倒序。

- 添加到侧边栏，放到邮箱管理的下面。

- 列表显示基础数据，点击显示详细数据。
