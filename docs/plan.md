# 实施计划

## 项目目标

- 使用 `Go + Gin + Vue3` 实现单管理员电子邮件客户端。
- 使用 `embed` 嵌入前端静态资源。
- 使用 `github.com/nzlov/utils/db` 作为数据库框架。
- 默认支持 `sqlite`，同时兼容 `postgres` 与 `mysql`。
- 支持 `IMAP`、`SMTP`、`POP3`，其中 `POP3` 仅做收件。
- 支持多邮箱账户、聚合列表、单邮箱查看。
- 使用 Go 协程处理多邮箱并发同步。
- 使用 `config.yaml`，支持默认值回落与环境变量强覆盖。
- 进入网站需要登录；默认管理员从配置导入数据库，后续仅校验数据库密码。
- 使用 `github.com/robfig/cron/v3` 实现定时同步调度。
- 提供 Docker 部署能力。

## 实施清单

- [x] 初始化 Go/Vue 项目骨架与基础配置
- [x] 实现配置加载、环境变量覆盖与 `config.yaml` 回落
- [x] 接入数据库、自动迁移与默认管理员初始化
- [x] 实现 `JWT + HttpOnly Cookie` 认证与基础后端路由
- [x] 实现邮箱账户模型、密码加密存储与基础 API
- [x] 实现 `cron` 调度器与多邮箱并发同步骨架
- [x] 实现 Gmail 风格前端基础页面与登录流程
- [x] 接入前端 `embed`、`Dockerfile` 与 `docker-compose`
- [x] 完成基础验证并整理使用说明

## 交付范围

- 提供可启动的后端服务。
- 提供可构建的前端页面骨架。
- 提供登录、邮箱账户管理、聚合列表基础能力。
- 提供同步调度骨架与后续扩展点。
- 提供容器化部署文件。

## 验证结果

- [x] `go build ./cmd/server`
- [x] `npm run build`
- [x] 启动服务后访问 `GET /api/status`
- [x] 使用默认管理员完成登录
- [x] 通过 API 新增邮箱账户并查询列表

## 第二阶段计划

- [x] 实现 `SMTP` 发信服务与 `/api/messages/send`
- [x] 实现 `IMAP` 增量拉取并写入本地消息表
- [x] 实现 `POP3 UIDL` 去重拉取并写入本地消息表
- [x] 扩展 `cron` 同步器，按协议调用真实同步逻辑
- [x] 增加邮件详情接口 `GET /api/messages/:id`
- [x] 实现写信页并接入发信接口
- [x] 完成发信与同步接口联调验证

## 第二阶段验证结果

- [x] 发信接口已走到 SMTP 拨号层，并返回真实连接错误
- [x] 同步接口已走到 IMAP 拨号层，并返回真实连接错误
- [x] 前端写信页构建通过
